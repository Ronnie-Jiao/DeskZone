# DeskZone Repository Instructions

- Before making any code or documentation changes, read `docs/修改记录.md` and review its newest dated entries first. Use those entries to understand recent work, then inspect the current source before editing; the log is context and does not replace the user's request.
- Follow `docs/WIKI.md`, especially its source, Debug preview, self-contained release, and installation rules.
- Make product changes in this source repository. Do not use installed binaries as an editing source.
- After completing each change, append a new entry to `docs/修改记录.md` with the completion time in Asia/Shanghai, the affected areas/files, and verification actually performed. Preserve older entries and do not claim unverified behavior.

# DeskZone 修改记录

记录源码库中已完成或已落盘的改动，时间统一使用中国标准时间（Asia/Shanghai）。最新记录放在最前面。新任务开始时先读最新记录，再按需查看此前记录和当前源码。

## 2026-09-24 18:08:35

- 修复当前 Explorer 布局下桌面组件被图标视图遮挡的问题：当全屏 `WorkerW` 是 `Progman` 子窗口且位于 `SHELLDLL_DefView` 后方时，改将 `SHELLDLL_DefView` 作为宿主；组件显示在桌面图标视图上方，仍处于普通应用窗口下方。
- 更新 `docs/WIKI.md` 与 `docs/ARCHITECTURE.md`，记录顶层 WorkerW、子级 WorkerW 和桌面图标视图的宿主选择规则。
- 验证：`dotnet build DeskZone.sln -c Debug --no-restore` 成功（0 警告、0 错误）；从源码 Debug 路径启动（PID 79220），运行时父窗口为 `SHELLDLL_DefView`（HWND `0x20220`），句柄可见；按 `Win+D` 的实际桌面截图中确认面板和分类内容显示。未修改安装版或发布包。

## 2026-09-24 17:45:32

- 修复桌面宿主选择遗漏：部分 Explorer 布局把全屏 `WorkerW` 放在 `Progman` 子窗口中，而非顶层兄弟窗口；`WindowsDesktopHostService` 现在也会检查该子窗口，避免组件退回到 `Progman` 后不显示。
- 现场诊断确认 Debug 进程（PID 5708）来自源码 Debug 路径；实际面板句柄虽可见，但挂在 `Progman`，且当前 Explorer 的全屏 `WorkerW` 是其子窗口。旧快捷方式附带的 `--preview` 在当前源码中不再切换显示模式。
- 验证：`DeskZone.Shell.csproj` Debug 构建成功（0 警告、0 错误），应用 `Compile` 目标成功，`git diff --check` 通过。完整解决方案构建因正在运行的 Debug 进程锁定输出 DLL 而失败；尚未重建/运行新 Debug 版，也未完成桌面截图验收。未修改安装版。

## 2026-09-24 01:53:40

- 汇总本源码库当前尚未提交的全部应用与文档改动：包括显示名称编辑及最近记录同步、删除快捷方式/文件后清理引用、桌面宿主修复、Debug 与 Release 单实例隔离，以及 Wiki/开发约定/仓库 AI 指引。
- 核对其他窗口与分支：当前仅有一个已登记 worktree；`origin/feature/wpf-workspace` 的唯一提交与主线补丁等价；旧 `origin/feature/desktop-workerw` 与主线分叉，合并会删除当前 51 个文件，因此保留该历史分支，不将其整支并入。
- 验证：Debug 与 Release 均构建成功（各 0 警告、0 错误）；当前 `tests/` 仅含人工验收说明，没有自动化测试项目，本次未运行人工 UI 验收。

## 2026-09-23 17:38:22

- 修复桌面内容看似消失时 Debug 预览被安装版接管的问题：`src/DeskZone.App/App.xaml.cs` 为 Debug 与 Release 使用不同的单实例锁，避免源码调试版启动时转而唤醒安装版并退出。
- 调整 `src/DeskZone.App/MainWindow.xaml.cs`：移除启动 650 毫秒后的强制脱离/重新挂接；保留加载时挂接和桌面宿主定时自修复，避免启动阶段主动拆除桌面窗口。
- 在 `docs/WIKI.md` 更新 Debug 预览说明：两种构建使用独立单实例锁，但仍共用用户数据；预览前应退出安装版，避免重复面板和并发写入。
- 诊断时只读确认当前安装版进程路径为 `%LOCALAPPDATA%\Programs\DeskZone\DeskZone.exe`，其窗口句柄在检查时可见且有桌面父窗口；没有修改安装目录，也没有并行启动 Debug 窗口以免两版同时写入同一份本地数据。
- 验证：`dotnet build DeskZone.sln -c Debug --no-restore` 成功（0 警告、0 错误），`git diff --check` 通过；本次未做实际桌面截图验收或测试套件运行。

## 2026-09-23 10:11

- 新建本修改记录文档，汇总最近一轮已经落盘的 DeskZone 源码改动。
- 新增仓库根目录 `AGENTS.md`：要求后续 AI 在修改代码或文档前先阅读本文件并查看最新记录，完成修改后再按中国标准时间追加记录。
- 在 `docs/WIKI.md` 和 `docs/DEVELOPMENT.md` 中加入修改记录入口与开发前阅读要求。
- 验证：检查现有 Wiki、开发说明和工作区状态；本次仅修改文档与开发指引，未运行构建或测试。

## 2026-09-21（具体时分未保留）

- 文件、文件夹和快捷方式支持双击修改 DeskZone 内的显示名称；名称写入应用数据，不改磁盘上的真实文件名或快捷方式路径，并同步更新最近打开记录。
- 为桌面宿主中的重命名编辑增加独立输入窗口，让文字输入获得键盘焦点；编辑时隐藏底层只读名称，避免两层文字重叠。Enter 保存、Esc 取消、点击空白处确认。
- 文件名称按内容自动换行，不再限制为两行；长名称会增高卡片并将后续图标向下排布。
- 通过 Windows 原生右键菜单删除文件或快捷方式后，同步清理 DeskZone 中对应的引用和最近打开记录；删除快捷方式不会删除它指向的目标。
- 当时验证：Debug 构建成功（0 警告、0 错误），现有测试命令成功完成；实际桌面指针验收未记录。
